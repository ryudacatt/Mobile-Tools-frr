# third_party

This directory is reserved for pinned source dependencies:

- `capstone`
- `radare2`
- `lief`
- optional `ghidra`, `ogdf`

Dependency cloning is deferred to the dependency setup step to keep Phase 1 minimal and buildable.

